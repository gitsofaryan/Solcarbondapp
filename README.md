
  # SolCarbon DApp Prototype

  This is a code bundle for SolCarbon DApp Prototype. The original project is available at https://www.figma.com/design/FIzj6CLhReCqgjFFVteceM/SolCarbon-DApp-Prototype.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  